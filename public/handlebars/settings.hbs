<!DOCTYPE html>
<html lang="en">
<head>
    <title>Settings</title>
    <link rel="stylesheet" href="../lib/bootstrap.min.css">
    <link rel="stylesheet" href="../styles/style.css">
    <script src="../lib/jquery-3.3.1.min.js"></script>
    <script src="../lib/bootstrap.min.js"></script>
    <script src="../js/settings.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <script>
        $(function () {
            $("#navbar").load("../handlebars/navbar");
        });
    </script>
</head>
<body>
<div id="navbar"></div>

<br/>
<div class="container">
    <div class="jumbotron">
        <h3>Settings</h3>
        <br/>
        <form>
            <div class="custom-file">
                <input type="file" class="custom-file-input" id="customFile">
                <label class="custom-file-label" for="customFile">Choose file</label>
            </div>
            <div class="row">
                {{#each this}}
                    <div class="form-group col-xl-6 col-lg-6 col-md-12 col-sm-12 mb-3">
                        <label for={{id}}>{{label}}</label>
                        <input type="text" class="form-control {{#if feedbackText}} is-valid {{/if}}" id="{{id}}" value="{{value}}" {{attributes}}>
                        {{#if feedbackText}}
                            <div id="{{id}}-feedback" class="valid-feedback">Shucks, check the formatting of that and try again.</div>
                        {{/if}}
                    </div>
                {{/each}}
            </div>
            <button class="btn btn-primary" type="submit">Save Changes</button>
            <button class="btn btn-danger" onClick="window.location.reload()">Cancel Changes</button>
        </form>
    </div>
</div>
</body>
</html>